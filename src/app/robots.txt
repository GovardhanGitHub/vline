export default function robots() {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/private/', '/admin/'],
    },
    sitemap: 'https://visuallinestudios.com/sitemap.xml', // Replace with your domain
  }
}